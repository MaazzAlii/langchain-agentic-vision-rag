# Install with: pip install -r requirements.txt
chromadb>=0.5.0
pdf2image>=1.17.0
Pillow>=10.0.0
streamlit>=1.35.0
requests>=2.31.0
python-dotenv>=1.0.0
langgraph>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0

# Uncomment if using Ollama:
# ollama>=0.2.0

# SYSTEM REQUIREMENT — not a pip package:
# pdf2image needs Poppler installed:
#   Windows: https://github.com/oschwartz10612/poppler-windows/releases
#            Extract → add bin/ folder to PATH → restart terminal
#   Linux:   sudo apt install poppler-utils
#   Mac:     brew install poppler
